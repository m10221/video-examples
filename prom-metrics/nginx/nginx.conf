worker_processes  1;

events { worker_connections 1024; }

http {
    server {
        listen 8080;
        location / {
            return 200 'ok\n';
            add_header Content-Type text/plain;
        }
        location /stub_status {
            stub_status;
            access_log off;
            allow all;
        }
    }
}
